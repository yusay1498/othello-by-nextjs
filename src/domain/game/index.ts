// 型のエクスポート
export type {
  Player,
  Cell,
  Board,
  Position,
  Direction,
  GameState,
  GameMode,
  PlayerColor,
  GameConfig,
  GameStatus,
  WinnerResult,
} from './types';

// 定数のエクスポート
export {
  BOARD_SIZE,
  TOTAL_CELLS,
  DIRECTIONS,
  CORNERS,
  POSITION_WEIGHTS,
} from './constants';
